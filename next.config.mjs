/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
};

const millionConfig = {
  auto: false,
  // if you're using RSC:
  // auto: { rsc: true },
};

export default nextConfig;
